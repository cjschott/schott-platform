# v1.0 Engineering Ledger

The ledger is ordered by execution dependency, not identifier allocation.

Canonical sequence: ENG-0001 -> ENG-0002 -> ENG-0004 -> ENG-0005 -> ENG-0006 -> ENG-0003

| ID | Work | State | Entry gate | Exit gate |
|---|---|---|---|---|
| ENG-0001 | Implement the root establishment lineage contract | Released as `v0.9.7` — implemented, independently reviewed, merged | Operator Root ceremony complete; [ADR-0014](../decisions/ADR-0014-root-establishment-lineage.md) accepted | Test-first fix independently reviewed, released, and merged |
| ENG-0002 | Make `validate-store` genuinely read-only | Released as `v0.9.8` — implemented, independently reviewed, merged | ENG-0001 merged and released as `v0.9.7` | Test-first fix independently reviewed, released, and merged |
| ENG-0004 | Fabric Runtime | **Implemented and integrated through Increment 12.** Increments 1–7 released as `v0.9.9`; increments 8–12 integrated into `main` at `70d206c9`, Increment 12 through PR #42. Release `v0.10.0` is authorised; the release record is integrating and **the tag is not yet created** | ENG-0001 and ENG-0002 merged and released | Runtime implemented and validated in reviewable increments |
| ENG-0005 | Capability Runtime | Blocked by Fabric Runtime | Fabric Runtime exists; ENG-0005 has its own accepted specification | Capability execution implemented and validated within the Fabric |
| ENG-0006 | Health Runtime | Blocked by Capability Runtime | Fabric and Capability Runtime exist and are validated | Health Runtime exists and is validated |
| ENG-0003 | TrustGateway cutover | Final production transition | Fabric, Capability, and Health Runtime validated; required subjects seeded | Gateway uses `trust-plane-runtime` with no silent fallback |
| ENG-0007 | Capability Scheduler | Future | Runtime prerequisites and a separate approved specification | Independently defined by its approved specification and plan |
| ENG-0008 | Distributed Fabric | Future | Runtime prerequisites and a separate approved specification | Independently defined by its approved specification and plan |

## ENG-0001 evidence

Implemented test-first against
[ADR-0014](../decisions/ADR-0014-root-establishment-lineage.md) and its
[contract specification](../trust/root-establishment-lineage.md).

- `RootAuthorityLineage` carries no decision or approval fields — they are
  absent from the model, so no code path can populate one.
- `TrustLineage` is unchanged: both `TDEC` identifiers remain required.
- `declare_root_authority` persists one `root-establishment` lineage, reusing
  the already allocated `TLIN`. No `TDEC` is created; no second `TLIN` is
  allocated.
- `validate-store` reports an authority whose lineage is missing, of the wrong
  type, malformed in any stored value, or naming a different authority. It
  repairs nothing.
- Stored records are validated by reconstruction, not by field name, so a record
  whose keys are right and whose values are nonsense cannot satisfy the rule.
  Parse, type, and datetime failures become deterministic findings rather than
  crashes.

**Independently reviewed at `3b929cc`, then merged to `main` as `de6de9f` via
pull request #26 on 2026-08-04.** The review was conducted and attested
**outside GitHub**: pull request #26 carries no GitHub review record, no review
comment, and was merged by its author. Provenance for the independent review is
the operator's attestation, not a repository-native artefact — recorded here so
nobody later reads the exit gate as satisfied by evidence that does not exist.

**Released as `v0.9.7` on 2026-08-04.** The annotated, unsigned tag targets
`19ec85c` on `main`, and that released tree contains the reviewed implementation
`3b929cc`. No GitHub Release, release branch, release commit, or release PR was
created — the tag alone is the release, matching every prior version.

ENG-0001 has therefore satisfied its exit gate.

The existing production store is unchanged: `TLIN-000001` is **not** backfilled,
and `validate-store` reports its absence as an accurate finding. Releasing the
code fix does not retroactively create the record; the backfill remains a
separate, unauthorised action.

## ENG-0002 evidence

Implemented test-first against the
[post-root runtime sequence plan](../superpowers/plans/2026-08-03-post-root-runtime-sequence.md).

`validate-store` created the store it was asked to describe. Opening a store
initialised it, so validating an absent path built the store root, all six
record directories, `sequences/`, and `indexes/`, then reported the result
valid — a verdict partly describing the validator's own side effects.

- Opening for read is now separate from initialising. `ImmutableStore` gained
  `open_for_read()`, which constructs no directory and sets no mode; the
  initialising path is unchanged for every write command.
- `command_validate_store()` uses only that path. Exit codes are unchanged: `0`
  when valid, `1` when problems are reported, `2` for an invocation error.
- No filesystem mutation occurs whether the target is absent, empty, valid, or
  malformed, and repeated validation returns identical output over identical
  paths, modes, sizes, timestamps, and digests.

**Merged through pull request #28 as `4493cbc`, and released as `v0.9.8` on
2026-08-04.** The annotated, unsigned tag targets the release-record merge
commit on `main`, whose tree contains the independently reviewed implementation
`4af34a0`. As with `v0.9.7`, the tag alone is the release: no GitHub Release, no
release branch, no separate release commit.

Verdict and exit-code semantics are unchanged. `validate-store` still returns
`0` when valid, `1` when problems are reported, and `2` for an invocation error,
and an absent store still reports valid — pre-existing behaviour that ENG-0002
deliberately preserved rather than widened.

No production trust record was read, written, or repaired; `TLIN-000001` remains
un-backfilled, and validation still reports its absence as an accurate finding.
ENG-0002 performed no production repair or backfill of any kind.

The non-transactional write risk recorded under ENG-0001 is **unchanged and
still deferred**. ENG-0002 does not own it: an orphan root establishment lineage
is still undetected by the authority-to-lineage rule, and this change makes
validation read-only rather than adding the reverse check.

## ENG-0004 evidence

Implemented in reviewable increments against
[ADR-0012](../decisions/ADR-0012-distributed-capability-fabric.md) and the
[Fabric Runtime design](../superpowers/specs/2026-08-04-fabric-runtime-design.md),
each increment reviewed independently before the next was authorised.

Delivered scope: governed capability-instance admission; lifecycle transitions;
route governance and supersession; prepared request identity; trust-scope
composition; the normative Fabric reconciliation those required; and CHOST/CINST
chain-integrity enforcement through the validated-read boundary, so a
predecessor chain C4 cannot read fails closed rather than yielding a head no
readable record supports.

- **Implementation head** — `c503022b6dbc07acf1d4f32dc0565cf7a9bdfc94`, the
  reviewed and accepted tip of `eng-0004-fabric-runtime`, and the commit every
  acceptance decision was taken against.
- **Integration merge** — `c0898a04d0d33e450d63a01304ec241bd76b06e3`, the
  two-parent merge of pull request #33 that placed ENG-0004 on `main`. Its first
  parent is the pre-merge `main` and its second is the implementation head; all
  24 ENG-0004 commits remain reachable under their original identities, and the
  merged tree is bit-identical to the implementation head.

The two are not interchangeable. The implementation head is what was reviewed;
the integration merge is where it entered `main`. As with `v0.9.7` and `v0.9.8`,
the annotated tag targets the release-record merge commit, which is neither of
them — no GitHub Release, no release branch, no separate release commit.

Validation on merged `main`: platform-model 1002 pass / 0 fail;
capability-fabric 508 pass / 0 fail; fabric-runtime 6868 pass / 0 fail / 0
tracebacks, run twice with byte-identical output; full validator 34/34, exit 0;
platform-model mutation backstop pass. All six pull-request checks and all six
post-merge `main` checks concluded successfully, and the pull-request Gitleaks
scan covered all 24 ENG-0004 commits and detected no leaks.

### What `v0.9.9` did and did not deliver

**`v0.9.9` stands.** It is the immutable release boundary recording the ENG-0004
implementation state that actually existed at that point. Nothing here rewrites,
moves, or replaces the tag, and no history rewrite is required or intended.

**It released increments 1–7 of a twelve-increment plan.** At the `v0.9.9`
boundary the [accepted implementation
plan](../superpowers/plans/2026-08-04-eng-0004-fabric-runtime-implementation-plan.md)
still had five increments outstanding, and **none of them was descoped**:

| Increment | Component | Planned module |
|---|---|---|
| 8 | C5 eligibility evaluator | `tools/fabric/eligibility.py` |
| 9 | C6 deterministic selection and the routing–selection boundary | `tools/fabric/selection.py` |
| 10 | C8 inspection and validation surface | `tools/fabric/inspection.py` |
| 11 | Fabric interface integration | `tools/fabric/cli.py` |
| 12 | Failure injection, concurrency, regression, documentation closure | — |

**An earlier wording of this section said "Increment 8 did not begin as part of
ENG-0004", and that sentence was wrong twice over.** It used "increment 8" to
mean work outside ENG-0004, when increment 8 in the accepted plan is the C5
eligibility evaluator — an ENG-0004 component. It also listed *selection* among
the things deliberately excluded, when **C6 deterministic selection is
ENG-0004's own increment 9**, not out of scope. A reader could have concluded
increments 8–12 were either finished or intentionally abandoned. Neither is
true.

**The scope statement that sentence was reaching for, stated correctly:** at the
`v0.9.9` boundary no Health Runtime, Capability Runtime, remediation, automatic
recovery, scheduling, placement, clustering, autonomous execution, or
TrustGateway work had begun, and every path the released increments introduced
refuses rather than acts.

**ENG-0004 remains active** for completion of accepted increments 8–12,
prospectively after `v0.9.9`. The accepted [design
specification](../superpowers/specs/2026-08-04-fabric-runtime-design.md) and
implementation plan remain authoritative and unchanged; no architecture was
rescoped, and **no downstream ENG item absorbs these responsibilities** — C5,
C6, C8, the interface, and final closure stay ENG-0004's.

Two non-blocking housekeeping observations are carried forward, neither a
release blocker and neither addressed here:

- `_successors.field` is unused — the body resolves the identity field by record
  kind instead. No runtime correctness or security consequence has been
  demonstrated; every call site passes a correct value and nothing reads it.
  Deferred for later cleanup.
- The Gitleaks workflow comment wording can be read as implying no suppression
  mechanism exists, while `.gitleaksignore` holds two entries pinned to a single
  commit, file, rule, and line each. Scanner behaviour is not weakened and the
  full ENG-0004 range scanned clean; the wording cleanup is deferred.

### Increments 8–12 are integrated

**ENG-0004 is implemented and integrated through Increment 12.** The five
increments outstanding at the `v0.9.9` boundary were implemented test-first,
each reviewed and accepted before the next was authorised:

| Increment | Component | Module |
|---|---|---|
| 8 | C5 eligibility evaluator | `tools/fabric/eligibility.py` |
| 9 | C6 deterministic selection | `tools/fabric/selection.py` |
| 10 | C8 inspection and validation surface | `tools/fabric/inspection.py` |
| 11 | Fabric interface integration | `tools/fabric/cli.py` |
| 12 | Failure injection, concurrency, regression, documentation closure | `tools/fabric/store.py` (request serialisation) |

Three architecture corrections were accepted along the way, each because an
accepted semantic was undefined rather than merely unimplemented: capability
scope was anchored at admission so ELIG-8 compares one namespace to itself; an
operator-supplied `local_node_identity` gave `local-only` locality something to
mean; and `CSEL` route provenance became conditionally required so a no-candidate
refusal is constructible. Increment 12 added the serialisation that makes one
request identity yield one decision under concurrency.

**Increments 8–11 were integrated on `main`** through pull requests #36, #37,
#38, #39, #40, and #41, each reviewed and merged under its own request.
**Increment 12 was integrated through pull request #42**, whose merge commit
`70d206c99b8b9d478bcb1a22336c2c12631cb512` is the ENG-0004 integrated baseline.
The Increment 12 branch had zero unique commits relative to `main` and was
deleted safely, locally and remotely.

Validation on merged `main` at `70d206c9`: docs-static 993 pass / 0 fail;
platform-model 1002 / 0; capability-fabric 514 / 0; fabric-runtime 7989 pass /
0 fail / 0 tracebacks, run twice with byte-identical output
(`sha256:7725778698e5ae0470d58a66e5ad430147927bb473dab3ac25f01ccd4c7d7e9c`);
ShellCheck pass; pre-commit pass; full validator 34/34, exit 0; platform-model
mutation backstop pass. All six post-merge checks concluded successfully.

### What ENG-0004 delivered

The integrated runtime implements every accepted component — **C1** immutable
store and request critical section, **C2** record validator, **C3** Trust
adapter, **C4** admission and lifecycle governance, **C5** eligibility, **C6**
deterministic selection, **C7** evidence, **C8** inspection and validation —
behind a **governed Fabric CLI exposing all twelve §8 operations**, with replay
and idempotency enforcement and the Increment 12 request-lock concurrency
hardening.

**What it does not contain is as much of the boundary as what it does.** There
is no Health Runtime and no Capability Runtime execution: nothing here runs a
capability, and no flag could make it. ENG-0005 and ENG-0006 have not begun, no
production subjects have been seeded, and no TrustGateway cutover has been
performed.

### Release boundary

**`v0.10.0` — "Schott Platform v0.10.0 - Governed Fabric Runtime" — is the
authorised release boundary** for ENG-0004 completion, covering increments 8–12.
It is a **new** boundary: `v0.9.9` stands unchanged as the historical boundary
for increments 1–7, and `v1.0.0` remains unavailable because the roadmap
requires later runtime work including the Capability Health Monitor.

**At the moment this record is written the release version is authorised, this
release record is still integrating, and the tag does not yet exist.** The
annotated `v0.10.0` tag targets this record's merge commit once its checks are
green.

## The defect sprint is complete; ENG-0003 has not started

Both released defects are now merged and released — ENG-0001 as `v0.9.7` and
ENG-0002 as `v0.9.8` — so the Fabric Runtime entry gate conditions are met.

**ENG-0004 released increments 1–7 as `v0.9.9` and completed increments 8–12,
integrated at `70d206c9`; nothing after it has begun.** ENG-0005 Capability
Runtime, ENG-0006 Health Runtime, and ENG-0003 TrustGateway cutover remain
unstarted and unauthorised. A
satisfied entry gate is permission to *specify* the next increment, not to
implement it: each still requires its own accepted specification, test-first
plan, review, and release boundary before any code is written.

The order is the one the [runtime sequencing
correction](0002-runtime-sequencing-correction.md) already records, and it is
unchanged: **ENG-0004 completion → ENG-0005 Capability Runtime → ENG-0006 Health
Runtime → required subject seeding → ENG-0003 TrustGateway cutover.** Identifier
order is not execution order — ENG-0005 precedes ENG-0006 because the Fabric
must place something before Health has anything to observe, and ENG-0003 is last
despite its low number.

## Capability Runtime is part of Fabric delivery

**ENG-0005 is an increment of the Fabric, not a separate plane.** ADR-0012
defines capability definitions, instances, packages, routes, and selection as
Fabric entities; executing them is the part of the Fabric that actually runs
work. ENG-0005 is therefore sequenced inside Fabric delivery — after the Fabric
engine exists (ENG-0004) and before Health has anything worth observing
(ENG-0006).

It remains a distinct ledger entry because it needs its own accepted
specification and its own review boundary — not because it is optional or
indefinitely deferred. **Its exact boundary against ENG-0004 is not yet
specified** and must be settled by that specification before implementation
begins.

## Two gates, not one

**TrustGateway cutover is intentionally not the Fabric Runtime gate.** The
Operator Root ceremony was.

**Fabric Runtime entry gate:** Operator Root ceremony complete -> ENG-0001
released -> ENG-0002 released.

**TrustGateway production cutover gate:** Fabric Runtime validated -> Health
Runtime validated -> initial subjects seeded -> verdict source ready -> rollback
validated -> deployment evidence retained -> TrustGateway cutover.

Cutover remains last so the runtimes can exist, be validated, and have their
subjects seeded before production traffic depends on them.

See [0002 - Runtime Sequencing Correction](0002-runtime-sequencing-correction.md)
and [ADR-0014](../decisions/ADR-0014-root-establishment-lineage.md).
