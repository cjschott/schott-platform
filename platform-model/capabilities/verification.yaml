---
# Verification capability.
#
# Maturity is a claim about implementation, not intent. See the rationale
# below for why this value was chosen.
#
# Governed by: docs/standards/capability-model-standard.md

api_version: schott-platform/v1
kind: Capability

id: CAP-0003
type: capability
name: Verification
slug: verification
lifecycle: active
owner: platform-engineering
maturity: foundation
risk: high
approval_required: false
review_required: true

provenance:
  class: declared
  source: docs/standards/capability-model-standard.md
  recorded_at: 2026-08-01
  last_reviewed: 2026-08-01

description: >-
  Compare declared intent against collected evidence and record a read-only finding.

maturity_rationale: >-
  Foundation: the result schema and validation exist; no evaluation engine has been built and no verification record has been produced.

review_note: >-
  Foundation: the result schema and validation exist; no evaluation engine has been built and no verification record has been produced.

inputs:
  - Entity records
  - Evidence records
  - Drift rules

outputs:
  - Verification results

dependencies:
  - CAP-0001
  - CAP-0002

implemented_by:
  - platform-model/schemas/verification.schema.yaml

validated_by:
  - tests/test-evidence-validator.sh

documented_by:
  - docs/standards/verification-drift-standard.md
  - docs/decisions/ADR-0002-evidence-first-architecture.md

relationships:
  canonical_source: platform-model/relationships/platform-capabilities.yaml
