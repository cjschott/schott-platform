---
# Drift Detection capability.
#
# Maturity is a claim about implementation, not intent. See the rationale
# below for why this value was chosen.
#
# Governed by: docs/standards/capability-model-standard.md

api_version: schott-platform/v1
kind: Capability

id: CAP-0004
type: capability
name: Drift Detection
slug: drift-detection
lifecycle: active
owner: platform-engineering
maturity: foundation
risk: high
approval_required: true
review_required: true

provenance:
  class: declared
  source: docs/standards/capability-model-standard.md
  recorded_at: 2026-08-01
  last_reviewed: 2026-08-01

description: >-
  Detect and report differences between declared and observed state as advisory findings.

maturity_rationale: >-
  Foundation: six rules are defined but none has been executed. All carry evidence_max_age null, so freshness cannot yet be enforced.

review_note: >-
  Foundation: six rules are defined but none has been executed. All carry evidence_max_age null, so freshness cannot yet be enforced.

inputs:
  - Verification results
  - Drift rules

outputs:
  - Drift findings
  - Recommendations

dependencies:
  - CAP-0003

implemented_by:
  - platform-model/drift-rules/core-platform.yaml
  - platform-model/schemas/drift-rule.schema.yaml

validated_by:
  - tests/test-evidence-validator.sh

documented_by:
  - docs/standards/verification-drift-standard.md

relationships:
  canonical_source: platform-model/relationships/platform-capabilities.yaml
