"""Shared infrastructure used by more than one platform layer.

Nothing here knows about evidence, integrity, experience, or occurrences. A
module belongs in this package only when at least two layers need it and it
carries no domain meaning of its own.
"""
