"""Capability Runtime (ENG-0005).

The execution layer downstream of governed Fabric selection. This increment is
the persistence foundation and nothing else: a second immutable store, in its
own plane, owing the Fabric nothing but its discipline.

**Nothing here executes a capability.** There is no adapter, and the absence is
a property this package is tested for rather than a stage it has not reached.
"""
